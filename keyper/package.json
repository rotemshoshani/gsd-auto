{
  "name": "keyper",
  "version": "0.1.0",
  "description": "Move secrets between dotenv files, Convex, and Vercel without exposing values.",
  "type": "module",
  "bin": {
    "keyper": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --exclude test/matrix.test.ts",
    "test:matrix": "npm run build && KEYPER_MATRIX_TESTS=1 vitest run test/matrix.test.ts --testTimeout=120000",
    "gate": "npm run check && npm test && npm run build"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "convex": "1.44.0",
    "dotenv": "16.6.1"
  },
  "devDependencies": {
    "@types/node": "22.18.6",
    "typescript": "5.9.2",
    "vitest": "3.2.7"
  }
}
